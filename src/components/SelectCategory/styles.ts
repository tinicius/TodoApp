import styled from "styled-components";

export const Button = styled.button`
  background-color: grey;
  color: white;

  width: 200px;
  height: 40px;

  margin-left: 10px;
  border-radius: 4px;

  border: none;
  padding: 0;
  font: inherit;
  cursor: pointer;
  outline: inherit;
`;
